import MockPageLayout from '@/components/MockPageLayout';

export default function ContactPage() {
    return (
        <MockPageLayout
            title="Communication // Uplink"
            subtitle="Terminal Contact"
            content="Send your encrypted inquiries or signal reports through the appropriate channels. Direct line availability is subject to network stability."
            status="READY_FOR_INPUT"
        />
    );
}
