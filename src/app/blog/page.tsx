import MockPageLayout from '@/components/MockPageLayout';

export default function BlogPage() {
    return (
        <MockPageLayout
            title="Archives // Signal Log"
            subtitle="The Slump Feed"
            content="A digital repository of frequency shifts, synthesis notes, and documented glitches in the sonic landscape. Stay tuned for periodic transmissions."
            status="AWAITING_SIGNAL"
        />
    );
}
